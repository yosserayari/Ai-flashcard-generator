Filter/control panel — key parameters to include:

Product/URL selector — add/remove competitor product links to track
Scrape frequency — options like hourly / daily / weekly, plus a "run now" manual trigger
Price change threshold — client sets what counts as "meaningful" (e.g. alert only if price drops more than 5%, not every tiny fluctuation)
Alert channel preference — email vs WhatsApp vs dashboard-only notification
Product grouping/tagging — label tracked products by category (useful once they're tracking more than a handful)
Date range on the dashboard — filter price history view by last 7/30/90 days
Currency/display preference — if competitors are in different currencies or formats
Active/paused toggle per product — stop tracking something without deleting its history

Nice-to-have, not essential for the demo:

Export data as CSV
Simple login/auth if you ever want it multi-client